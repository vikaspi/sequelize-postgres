// Can Create different config depends on different server

const DATABASE_NAME = 'postgres'
const DATABASE_USERNAME = 'postgres'
const DATABASE_PASSWORD = 'root'
const DATABASE_PORT = 5432
const DATABASE_SCHEMA = 'sequelize-postgres';

module.exports = {
    DATABASE_NAME,
    DATABASE_USERNAME,
    DATABASE_PASSWORD,
    DATABASE_PORT,
    DATABASE_SCHEMA
}
